@{
    Layout = null;
    var empresa = ViewBag.Empresa;
    var numero = ViewBag.NumeroPedido;
}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Pedido @numero - Nueva Visi&oacute;n</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: #f5f5f5;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }
        .card {
            background: white;
            border-radius: 12px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.1);
            padding: 40px;
            max-width: 480px;
            width: 100%;
            text-align: center;
        }
        .logo { font-size: 24px; font-weight: 700; color: #1a73e8; margin-bottom: 24px; }
        .pedido-numero {
            font-size: 48px;
            font-weight: 700;
            color: #202124;
            margin: 16px 0;
            user-select: all;
            cursor: text;
        }
        .pedido-label { font-size: 14px; color: #5f6368; text-transform: uppercase; letter-spacing: 1px; }
        .empresa { font-size: 14px; color: #5f6368; margin-bottom: 24px; }
        .info {
            font-size: 15px;
            color: #5f6368;
            line-height: 1.6;
            margin-top: 24px;
            padding-top: 24px;
            border-top: 1px solid #e0e0e0;
        }
    </style>
</head>
<body>
    <div class="card">
        <div class="logo">Nueva Visi&oacute;n</div>
        <div class="pedido-label">Pedido</div>
        <div class="pedido-numero">@numero</div>
        <div class="empresa">Empresa @empresa</div>
        <div class="info">
            Para ver el detalle de este pedido, abre <strong>NestoApp</strong> en tu dispositivo Android
            o busca el pedido en <strong>Nesto</strong> escritorio.
        </div>
    </div>
</body>
</html>
